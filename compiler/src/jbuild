(jbuild_version 1)

(executable
	((name main)
	(preprocess (pps (sedlex.ppx ppx_driver.runner -no-check)))
	(libraries (core menhirLib sedlex))))

(install
	((section bin)
	 (files ((main.exe as procc)))))

(menhir
	((flags (--explain))
	(modules (parser))))
